@model IEnumerable<CadastroAulas.Models.AulaAluno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aluno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codcred)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Aluno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Codcred)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.key">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.key">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.key">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
